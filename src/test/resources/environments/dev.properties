#phantom,chrome,ie,htmlunit
browser=chrome
headless=false
timeout=20
selectors.version=V1
display.stacktrace=true

# application list

# proxy configuration
http_proxy=
https_proxy=
no_proxy=

# authentication type (BASIC or empty)
crypto.key=my-secret

locale=en