language: java
sudo: required
dist: xenial
install: true

services:
- postgresql
- mysql
- xvfb

addons:
  apt:
    packages:
      - dpkg
  postgresql: "9.4"
  sonarcloud:
    organization: "noraui"
  firefox: latest
  chrome: stable
  
jdk:
- oraclejdk8
- openjdk8
- oraclejdk11
- openjdk11

before_script:
- chmod +x test/run.sh
- chmod +x test/after_success.sh
- psql -c 'create database noraui owner travis;' -U postgres
- psql -U travis -d noraui -a -f config/noraui-db.sql
- mysql -e 'CREATE DATABASE noraui DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;'
- mysql --user=travis noraui < config/noraui-db.sql > output.tab
- export AUTHOR_NAME="$(git log -1 $TRAVIS_COMMIT --pretty="%cN")"

script:
- test/run.sh

after_success:
- test/after_success.sh

cache:
  directories:
    - "${HOME}/.m2"

notifications:
  webhooks:
    urls:
      - https://webhooks.gitter.im/e/b8f036338cae1f916682
    on_failure: always  # options: [always|never|change] default: always
